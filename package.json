{
  "name": "synchrono-city",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "pnpm --filter web build",
    "dev": "pnpm --filter web dev",
    "dev:backend": "docker compose up postgres redis livekit blossom migrate concierge relay",
    "test": "pnpm --filter web test",
    "verify": "pnpm --filter web build && pnpm --filter web test && cd apps/concierge && go test ./..."
  }
}
